# PAPER_TRADE_GAPS — Réconciliation resolver (2026-07-19 23:32 UTC)

> Écarts constatés entre le prompt de mission (`PROMPT_OPUS_RECONCILIATION_PAPERTRADE_20260720.md`)
> et l'état réel du code au HEAD `334ccf6`. **Documenté, NON corrigé** (R22 strict, §3 du prompt).

## 1. Le diagnostic « TP/SL fixes 5.5/-17.5 » est périmé

Le prompt (§1.2) affirme que `PaperTradeLoop` applique un TP/SL fixe `5.5/-17.5`.
**Introuvable dans le code au HEAD actuel.** La réalité observée :

- `core/v9/exit_simulator.py` : résolution DYNAMIC **par session** déjà en place
  (asie TP10/SL15, london TP8/SL15, overlap TP5/SL15, new_york/after scaling réduit).
- `scripts/v9_close_paper_trades.py` (ligne 71-77) : la clôture réelle copie
  `decisions.is_win` et écrit `pips_simulated = +10.0 (win) / -10.0 (loss)` —
  valeur **symbolique**, pas un TP/SL.

→ La divergence mesurée (WR 23.8% paper vs 87.3% decisions, §1.1) ne vient donc
  **pas** d'un TP/SL 5.5/-17.5, mais très probablement de `paper_trades` clôturés
  par un **mécanisme antérieur** (avant le fix de copie is_win) coexistant avec des
  lignes récentes copiées de `decisions`. À investiguer hors périmètre.

## 2. Colonnes de contexte absentes de `paper_trades`

Le prompt suppose (§A.1) que le trade porte `vol_regime`, `session`, `timeframe`.
**Faux** : `paper_trades` = `(trade_id, snapshot_id, direction, confiance,
principes_source, opened_at, closed_at, pips_simulated, is_win, risk_go_context)`.

- `symbol`, `timeframe`, `regime_type` ne vivent que dans `decisions` (jointure
  `snapshot_id`).
- `vol_regime` **n'existe nulle part** en colonne. Le resolver le **projette**
  depuis `regime_type` (`_regime_bucket`) — proxy, pas une mesure directe.
- `session` est **inférée** de l'heure UTC d'ouverture (`infer_session_from_hour`).

→ Impact : `ResolutionContext` est reconstruit best-effort au moment du shadow.
  Une vraie colonne `vol_regime` snapshottée à l'ouverture serait plus fidèle.
  **Non ajoutée** (interdiction de migration DB, §3).

## 3. Fichiers du prompt qui n'existent pas sous le nom cité

- `core/v9/v9_decision_resolver.py` — **absent**. Le moteur DYNAMIC de référence
  est réparti entre `exit_simulator.py` / `trade_engine.py`. Aucune modification
  faite (le moteur DYNAMIC reste la référence intouchée, §3).
- `core/v9/_load_shared_context.py` (P3-WIRE) — le resolver tente un import
  best-effort ; si absent, `_adaptive_threshold=None` (aucun impact déterministe).

## 4. `calibrate_from_history` — limite de provenance

`decisions.resolution_pips` est traité comme **proxy d'excursion finale**, pas
comme une re-simulation intrabar OHLC (seul signal disponible sans rejouer les
snapshots). La table calibrée reflète la distribution des résolutions DYNAMIC
passées, pas la dynamique tick-par-tick. Documenté dans la docstring du module.

## 5. DoD §5 critère 6 — exit code audit

Le critère attend `audit --dry-run` → exit 0. Sur données live, `drift_max = 100%`
(plusieurs contextes n=1) → exit **2** (comportement correct et voulu : le code de
sortie reflète la sévérité du drift). Ce n'est pas un échec — c'est la confirmation
empirique de la prémisse d'audit.

---
_Gaps documentés, hors périmètre de cette livraison. Aucun fix appliqué (R22)._
