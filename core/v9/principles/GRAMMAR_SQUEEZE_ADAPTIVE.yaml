id: GRAMMAR_SQUEEZE_ADAPTIVE
version: 1
origin: V9-P3-CONSUME-EXTEND
status: SHADOW
v9_status: SHADOW
kind: grammar
created_by: hermes_v9
created_at: '2026-07-14'
promoted_at: null
scope:
  timeframes: ALL
  currencies: ALL
conditions:
- field: adaptive_pliure_threshold
  op: is_not_null
  value: true
- field: compression_extension_etat
  op: ==
  value: compression
- field: zone_type
  op: '!='
  value: respiration
emits: {}
bounds: {}
anti_signal_bias: false
notes: 'P3-CONSUME-EXTEND (2026-07-14) — variante _ADAPTIVE du principe GRAMMAR_SQUEEZE (origin=V8_NATIVE).
  Stratégie : pliure. Consomme les seuils adaptatifs posés par P3-WIRE (adaptive_pliure_threshold) via
  3 conditions de garde ''is_not_null'' en tête. Dégradation gracieuse R6 : si P3-WIRE est OFF (kill switch
  V9_ADAPTIVE_THRESHOLDS_WIRED_ENABLED=0), les champs adaptatifs sont absents du context, les gardes sont
  false, le principe ne déclenche pas. SHADOW par défaut (R25''), promotion ACTIVE = motion CEO distincte
  + DECISIONS_LOG.


  Source originale : Mot de grammaire ''SQUEEZE'' — source V8 calc_cycle_phase.py

  (std_df >= 5.0 ET range <= 0.8 pip). Adapté V9 via

  compression_extension_etat=compression + zone_type!=respiration

  (distinction SQUEEZE vs LOCK par absence de respiration).

  SQUEEZE = pression accumulée sans release (std eleve).

  Conditions écrites 2026-07-10 (Hermes diagnostic V8 → V9).'
