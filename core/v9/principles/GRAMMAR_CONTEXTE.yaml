id: GRAMMAR_CONTEXTE
version: 3
origin: V6
status: ACTIVE
v9_status: ACTIVE
kind: grammar
created_by: claude_code
created_at: '2026-06-12'
promoted_at: '2026-07-16'
scope:
  timeframes: ALL
  currencies: ALL
conditions:
- field: marche_ouvert
  op: ==
  value: true
- field: session_marche
  op: in
  value:
  - london
  - new_york
  - overlap
- field: contexte_temporel_fenetre
  op: is_not_null
emits: {}
bounds:
  coalition_mtf_score:
    min: 2
    max: 6
  risk_confidence:
    min: 60
    max: 100
anti_signal_bias: false
notes: "Mot de grammaire 'CONTEXTE' — source pf_grammar.py, TF M15/M5, statut doc=VALID.\n\
  Entrée documentaire (kind=grammar, non émetteur). Promotion ACTIVE conditionnée\
  \ à WR V8 >= 50%.\n\nENRICHISSEMENT CONTEXTE 2026-07-06 (13 nouveaux champs):\n\
  - session_marche in [london, new_york, overlap] -> liquidite haute, signal favorise\n\
  - session_marche == asie -> liquidite basse, bruit, signal malus\n- heure_utc >=\
  \ 15 ET jour_semaine == 4 (vendredi >= 15h UTC) -> vigilance fin de semaine\n- marche_ouvert\
  \ == false -> aucun signal possible (fallback safe)\n\nENRICHISSEMENT CONTEXTE 2026-07-06\
  \ (Session 5 — P2 DORMANT promovus):\n- contexte_temporel_fenetre in [ouverture\
  \ de session, mi-session, cloture de session]\n  -> granularite intra-session pour\
  \ affiner CONTEXTE (DORMANT P2 maintenant PROPAGÉ)\n\nCORRECTIF Phase 9.8 Phase\
  \ B (2026-07-08, audit §5.2) : transcription des 3\nconditions de gating (marche_ouvert,\
  \ session_marche, contexte_temporel_fenetre)\ndans `conditions:` — cas jugé le plus\
  \ mûr par l'audit (17 champs déjà\ndocumentés en note, tous PROPAGÉS CONTEXT_CONTRACT.md).\
  \ La nuance\n\"heure_utc >= 15 ET jour_semaine == 4 -> vigilance fin de semaine\"\
  \ reste\ninformative (modificateur de confiance, pas un gate dur) : elle n'est pas\n\
  transcrite en `conditions:` pour ne pas éteindre le principe 6 jours sur 7.\nReste\
  \ v9_status=SHADOW (règle 25' — promotion hors périmètre de ce correctif).\n"
promoted_by: mandat_CEO_boucle_fermee
strategy:
  tp_pips: 8
  sl_pips: 12
  sizing_multiplier: 0.8
  min_confidence: 55
