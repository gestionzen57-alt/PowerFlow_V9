id: SIGNAL_OPEN
version: 2
origin: V9-meta-agent
status: ACTIVE
v9_status: ACTIVE
kind: grammar
created_by: hermes_v9
created_at: '2026-07-10'
promoted_at: '2026-07-14'
scope:
  timeframes: ALL
  currencies: ALL
conditions:
- field: window_statut
  op: ==
  value: ouverte
- field: confiance_qualification
  op: '>='
  value: 70
emits: {}
bounds:
  confiance_qualification:
    min: 70
    max: 100
anti_signal_bias: false
notes: |
  Mot de grammaire 'SIGNAL_OPEN' — déclencheur sur l'event meta-agent signal_open.

  Origine : proposition meta-agent V9 (commit a726dee), 5 patterns détectés
  signal_open sur 24h, confiance 0.60 (new_yaml_shadow).

  ENRICHISSEMENT CEO 2026-07-10 :
  - window_statut == ouverte : gate RiskManager #4 (champ réel posé par
    principle_engine._load_shared_context — anciennement mal nommé
    window_status, corrigé 2026-07-14 audit ZCode).

  FIX DIVERSIFY 2026-07-16 (Claude Opus, diagnostic 67k lignes windows) :
  la condition testait window_statut == "exploitable", valeur JAMAIS
  produite pour window_statut (confusion avec exploitability.statut).
  Valeurs réelles de windows.statut : absente/ouverte/invalidee/ambigue/
  fragile. Le statut "fenêtre ouverte" = "ouverte" (411 lignes). Correction
  vers "ouverte" — le principe passe de 0 déclenchement à un taux sain.
  - confiance_qualification >= 70 : aligné avec RiskManager.CONFIANCE_MIN
    (champ réel posé ligne 766 — anciennement mal nommé confiance, corrigé
    2026-07-14 audit ZCode).

  Champ `action` (preparer_entree) : DORMANT — non posé dans
  _load_shared_context(). Conditions sur `action` retirées (principe mort
  par construction tant que le champ n'est pas propagé). Si propagation
  future, ajouter la condition `action == preparer_entree`.

  Statut SHADOW (R25') : vocabulaire descriptif, pas une promotion ACTIVE.
strategy:
  tp_pips: 8
  sl_pips: 15
  exit_strategy: TP_SL
  max_hold_bars: 12
  sizing_multiplier: 0.5
  min_confidence: 80
  allowed_sessions:
  - asie
  - london
  anti_correlation: true
