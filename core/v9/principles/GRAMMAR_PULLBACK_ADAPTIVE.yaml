id: GRAMMAR_PULLBACK_ADAPTIVE
version: 1
origin: V9-P3-CONSUME-EXTEND
status: ACTIVE
v9_status: ACTIVE
kind: grammar
created_by: hermes_v9
created_at: '2026-07-14'
promoted_at: '2026-07-16'
scope:
  timeframes: ALL
  currencies: ALL
conditions:
- field: adaptive_pliure_threshold
  op: is_not_null
  value: true
- field: bascule_detectee
  op: ==
  value: false
- field: bascule_intensite
  op: <=
  value: 30
- field: qualification
  op: is_not_null
emits: {}
bounds:
  bascule_intensite:
    min: 0
    max: 50
anti_signal_bias: false
notes: "P3-CONSUME-EXTEND (2026-07-14) — variante _ADAPTIVE du principe GRAMMAR_PULLBACK\
  \ (origin=V6). Stratégie : pliure. Consomme les seuils adaptatifs posés par P3-WIRE\
  \ (adaptive_pliure_threshold) via 3 conditions de garde 'is_not_null' en tête. Dégradation\
  \ gracieuse R6 : si P3-WIRE est OFF (kill switch V9_ADAPTIVE_THRESHOLDS_WIRED_ENABLED=0),\
  \ les champs adaptatifs sont absents du context, les gardes sont false, le principe\
  \ ne déclenche pas. SHADOW par défaut (R25'), promotion ACTIVE = motion CEO distincte\
  \ + DECISIONS_LOG.\n\nSource originale : Mot de grammaire 'PULLBACK' — source pf_zone_breathing_topology,\
  \ TF H1, statut\nENRICHISSEMENT PULLBACK 2026-07-06 (Anomalies #1 #3):\n- bascule_detectee\
  \ == false -> pullback sur bascule en cours = piege\n- bascule_intensite < 30 ->\
  \ pullback sur conflit faible = signal fiable\ndoc=DORMANT. Entrée documentaire\
  \ (kind=grammar, non émetteur). Promotion ACTIVE\nconditionnée à WR V8 >= 50%.\n\
  \nENRICHISSEMENT PULLBACK 2026-07-06 (Session 5 — P2 DORMANT promovus):\n- point_de_rupture_declencheur\
  \ (str) -> declencheur qualitatif du pullback (pliure, bascule, etc.)\n- est_variante\
  \ (bool) / comportement_reference (str) -> detection variante de comportement connu\n\
  \  (ex: pullback en espece vs pullback en range), documente la nature du pullback\n\
  \nCORRECTIF Phase 9.8 Phase B (2026-07-08, audit §5.2) : transcription des 3\nconditions\
  \ déjà rédigées en note (anomalies #1/#3) dans `conditions:` :\nbascule_detectee==false\
  \ (pas de pullback pendant une bascule en cours =\npiège), bascule_intensite<=30\
  \ (seuil \"repère\" R29 §3.2, note disait <30 —\nl'opérateur `<=` est le plus proche\
  \ disponible côté moteur, cf.\n_KNOWN_OPS). Reste v9_status=SHADOW (règle 25' —\
  \ promotion hors\npérimètre de ce correctif).\n\nREFONTE Phase 14b (2026-07-08,\
  \ post-diagnostic Phase 14a) :\ncondition `persistance_confirmee == true` substituée\
  \ par\n`qualification is_not_null`. Justification : `persistance_confirmee`\nest\
  \ un DORMANT non-propagé (défaut False dans\ncore/v9/principle_engine.py L485-487,\
  \ jamais calculé par le\npipeline actuel) → la condition était structurellement\
  \ impossible à\nsatisfaire (0/100 triggers, BOTTLE_NECK_IDENTIFIED). `qualification`\n\
  est propagé par `_load_shared_context` (fallback None), alimenté\npar behavior_analyzer\
  \ pour les snapshots avec behavior. La nouvelle\ncondition capture l'intent original\
  \ (\"signal de pullback avec\nbehavior qualifié\") tout en étant techniquement évaluable.\
  \ Promotion\nACTIVE reste conditionnée à R30 + R25' (hit_rate ≥60% sur ≥50 décl.).\n\
  Refonte tracée : DECISIONS_LOG §« Phase 14b — refonte GRAMMAR_PULLBACK »."
promoted_by: mandat_CEO_boucle_fermee
strategy:
  tp_pips: 10
  sl_pips: 15
  exit_strategy: TP_SL
  max_hold_bars: 12
  sizing_multiplier: 1.0
  min_confidence: 60
  allowed_sessions:
  - asie
  - london
  preferred_regime:
  - CASSURE
  - EXTENSION
  anti_correlation: true
