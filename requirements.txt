# PowerFlow V10 — requirements.txt
# Doctrine V10 (2026-08-05, CEO mandate "no limit") : stack quant complète.
# Installée dans l'interpréteur qui exécute les tests (Hermes venv).
# Note pandas-ta : requiert Python 3.12 (indisponible sur 3.11) → fallback finta.
numpy>=1.24
pandas>=2.0
scipy>=1.10
statsmodels>=0.14
scikit-learn>=1.4
hmmlearn>=0.3
ruptures>=1.1
arch>=7.0
plotly>=5.0
finta>=1.3
pytest>=7.0
python-dotenv>=1.0
sqlite-utils>=3.0
MetaTrader5>=5.0.50
